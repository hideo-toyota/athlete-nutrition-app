# CLAUDE_HANDOFF — Codex第N次監査の統合裁定・スコープ規律(司令塔用)

クラウド側裁定者より。Codex(アップグレード版)の「分析精度・証券アナリスト機能・AI役割分離」
監査を受理。**10項目中6項目は既決の再発見**。新規4項目のみ裁定し、再発見の再発防止を定める。

## 1. 既決(再発行不要・実行待ち)
- #1判断採点=Phase 2c-4 / #2 CA混入=Phase 2c-3 / #3相対採点=2c-4+指数取得 /
  #4 M0=v2のD0/SOURCE_CONTRACT / #5決算修正=HB-003 / #9機械可読manifest=P1(バッチ後)
- これらは「新SPEC不要・既存フェーズで実装待ち」。重複裁定しない。

## 2. 新規裁定(4項目)
### #6+#7 ファンダの深化 → Phase 3 の取得項目リストとして定義(新フェーズ作らない)
- fins/details 実装時の取得・分析項目に以下を含める(論点1/2の計器):
  営業CF品質(利益とCFの乖離)/ 運転資本の増減 / 希薄化(発行株数変化・潜在株) /
  自己株式(取得・消却)/ 資本配分(配当性向・投資)/ セグメント情報。
- **自己株・発行株数変化**は需給(論点4)にも効くため cross-tag。大株主・信用・空売りと
  同じ「需給統合」ビューに将来まとめる(統合ビューのSPECはPhase 3後半)。
- Phase 3 SPEC発行時に本リストを反映(今は取得しない)。

### #8 ニュース4分類(novelty/official_confirmation/price_reaction/sector_diffusion)
- **保留継続**(以前の「情報性質スコア」と統合)。次にニュースレーンを触る時のスキーマ候補として
  BATCH_REVISION_QUEUE に保管。今は実装しない。

### #10 Codexセッションの役割分離 → 採用・ROLES v2 に組み込む
- **長期セッション = 履歴監査・翻訳・設計対抗提案**(git非接触)/ **実装 = タスク別の新規セッション**
  (書き手宣言と対・完了後クローズ)。「セッションは知識の仮の住まい」原則に合致。
- ROLES v2 の Codex 節に明記(裁定者の宿題)。

## 3. スコープ規律(重要・再発防止)
- **Codexに監査を依頼する前に、必ず BATCH_REVISION_QUEUE.md と直近の裁定文書(incoming/)を
  読ませる**。既決項目の再発見を防ぐため。以後の監査依頼プロンプトにこの前提を含める。
- 裁定者の方針: **現時点で新フェーズ・新レーンを増やさない**。指摘・提案は飽和しており、
  不足しているのは実行。次のCodex起用は「新監査」でなく「**Phase 2c-1実装の対抗提案**」。

## 4. 実行順(変更なし・再掲)
1. G1残務の是正(進行中)
2. Phase 2c(2c-1 snapshot → 2c-2 readiness → 2c-3 CA隔離 / 指数取得 → 2c-4 判断採点)
3. バッチ改版(ROLES v2[#10反映]・INDEX v2[#9解消]・judgment/gate発効)
4. Phase 2b 米国M0(SOURCE_CONTRACT→実装)
5. Phase 3(fins/details[#6+#7の項目リスト]・大株主)
6. G2以降(DT-1c・HB-001/002/003検証)

## 5. 期待効果・費用・リスク(裁定者の評価)
- 最大の期待効果は #6(ファンダ深化)と Phase 2c(読み取り層)= オーナーの「浅い」への
  直接の解。費用は Phase 3 の取得実装(中)。誤検知リスクは低(取得・表示層で予測を伴わない)。
- #8・機械可読化は効果中・費用中で後回しが妥当。#10は費用小・統治効果中で即ROLES v2へ。

規律不変: 新フェーズ増設は慎重に / 実行 > 発見 / market_view検証は仮説 / push無し(報告リポ)。
