# CLAUDE_HANDOFF — 棚卸し: 既存USレーンとセクターアナリストspec(司令塔用・読み取りのみ)

クラウド側裁定者より。米国地合いレーンM0とセクター別アナリストの仕様発行(G1後)に先立ち、
**既にMacに存在する2つの資産の現状**を報告してほしい。実装・変更はしない。読み取りのみ。
G1作業と並行してよい(衝突しない)。

## 1. us_market / us_evidence レーンの棚卸し
以下を確認依頼MD形式で(私はMacファイルを見られないため、要約でなく**転記**で):
1. 何を取得しているか: データソース(URL/API)・指標(指数/金利/為替/VIX等の別)・
   raw/derived の保存先とスキーマ(1レコード例を転記)
2. 実行頻度とトリガ(LaunchAgent? 手動?)・最終実行日・直近の成功/失敗
3. 出力先: brief・daily-update・Discord への接続の有無(接続コードの file:line)
4. PIT・provenance・鮮度ゲートの有無
5. テストの有無と件数
6. このレーンを作った主体と経緯(コミットhashと日付)

## 2. SECTOR_ANALYST_SPEC.md の棚卸し
1. **全文を転記**(私が読むため)
2. 状態: 仕様のみか、実装・稼働があるか(あれば該当コード・出力例の場所)
3. 作成主体と日付(git log)
4. 既存5班(J-Quants/kabutan/nikkei/newspicks/EDINET)との重複・整合の初見(1〜2行でよい)

## 3. 関連確認(1行ずつでよい)
- orchestrator統合ノート(2026-07-06-JP-orchestrator-integrated.md 等)に米国市況の
  記述が既に入っているか
- codex_selection_log / us_evidence 由来のデータが日本株の判断材料に接続されている箇所が
  あるか(あれば file:line — シグナル接続の有無の統制確認)

## 4. 報告
確認依頼MD形式で radar-ops/reports/ へ。G1報告と別便でも同梱でもよい(相区分すること)。
この報告を受けて、裁定者が Phase 2b(米国地合いM0)仕様と、セクターアナリストの採否・
配置(Phase 3-4圏)を確定する。

規律不変: 読み取りのみ / push無し / 売買推奨・ランキングなし。
