# CLAUDE_HANDOFF — 障害対応: education空投稿の診断と完全性ゲート実装(司令塔用)

クラウド側裁定者より。オーナー報告: 19:00 の education 投稿が**フロントマターのみで本文ゼロ**
(date: 2026-07-06 / serial: 4 / concept: D-統計リテラシー)。さらに serial が 4 に進んでおり、
**#2(B)・#3(C)が配信されないまま消費された疑い**がある。ロードマップの 0-4 を本タスクで
置き換える(DoDクローズは本修正の完了後)。

## 1. 診断(読み取りのみ・先に全部やる)
1. education レーンのログ(リポ外)から、**全発火の履歴**(日時・exit code・stderr)を列挙。
   serial 1→4 が何回の発火で進んだかを特定。
2. outputs/obsidian/education/ の全ノートを確認: 本文(題材/教養/Q1-Q3/解答部)が
   入っているのはどれか。B・C・D は本文があるか、殻だけか。
3. headless Claude 呼び出しの失敗理由(タイムアウト/認証/空レスポンス/ガードによる隔離)を
   ログから特定。material-guard が発動していた場合はその記録も。
4. 入力の状況: 7/5(日曜)・7/6 の source_artifacts が薄い/欠けていたか
   (週末は brief が無い。入力欠損が生成失敗の引き金かの確認)。
5. state ファイル(education_state.json)の現在値と、**state前進が「生成成功後」か
   「発火のたび」か**をコードで確認(ここが疑いの本丸)。

## 2. 修正仕様(単独コミット)
### E-FIX1. 完全性ゲート(投稿前バリデーション)
- 生成ノートに必須要素が**すべて**存在する場合のみ、公開部を Discord に投稿する:
  題材セクション / 教養セクション / Q1・Q2・Q3 / 解答マーカー(公開部の境界)。
- 1つでも欠けたら: **投稿は失敗通知1行のみ**
  「⚠️ 本日の判断力トレーニングは生成に失敗しました(理由: <短く>)。明日リトライします」
  ノートは .failed サフィックスで保存(消さない)。ledger に status=failed で追記。
### E-FIX2. state・serial の規律
- **state前進(分野ローテ・serial加算)は完全性ゲート通過後のみ**。失敗時は進めない。
- 失敗が3日続いたら失敗通知に連続日数を明示(未回答リマインドと同じ思想)。
### E-FIX3. 入力欠損への耐性
- 週末・休日で入力がゼロでも生成は止めない: 設計どおり「今日は◯◯が欠けていた」自体を
  題材にできることをheadlessプロンプトに明示(教養+一般化した3問は入力ゼロでも作れる)。
- headless 呼び出し失敗時は1回だけリトライ(間隔60秒)。
### E-FIX4. 巻き戻し裁定
- 未配信のまま消費された B/C/D は**消費していないものとして扱う**: state を「次=B」に戻す。
- serial は次の正常配信を **#2** とし、欠番(実際に飛んだ番号)は ledger に
  status=failed の行として正直に残す(無かったことにしない)。

## 3. テスト要件(合成)
- 本文が空/Q3欠落のノート → 投稿されない・stateが進まない・失敗通知が出る・.failed保存
- 完全なノート → 投稿され、stateが進む
- 入力ファイルゼロ → 生成プロンプトが「欠損を題材化」モードで組み立てられる
- 失敗3日連続 → 通知に「3日連続」が入る
- 既存 education テスト(17件)全通過+全スイート回帰なし

## 4. DoD・報告
- 全テスト通過 / 実発火1回で正常配信(本文入り)を目視 / 単独コミット
- 確認依頼MDに: 診断結果(§1の1〜5、特にstate前進のタイミングのコード該当行)/
  修正コミットhash / 巻き戻し後のstate値 / 次回配信予定(#2・分野B)
- 本タスク完了をもってロードマップ 0-4(education DoDクローズ)とする。

規律不変: 失敗を隠さない(このシステムの本体)/ 売買推奨なし / 秘密非表示 / push無し。
