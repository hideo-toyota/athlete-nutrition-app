# CLAUDE_HANDOFF — G0合格・DT-1b GO・付帯裁定(司令塔用)

クラウド側裁定者より。G0報告(CONFIRM_g0_phase0_20260707.md / 67d3269)を受理した。
**判定: 合格。Phase 0 をクローズし、Phase 1(DT-1b)に進む。**

## 1. G0の評価
- 0-4 education修正(fc5e427): 真因2層(awk打ち切り+無条件state前進)の特定と、
  実発火での新旧対比(2371字 vs 殻231字)による実証は模範的。時刻外配信の正直な申告も良い。
- §7配備 8/8 MATCH: 第2監査役のブロック解除を確認。
- D0-R: 「403=プラン除外」の前回判定の訂正が実測で確定。誤りを上書きした記録が
  attempts_log 的に残っている構成でよい。
- 0-3(watchlist/Codex)は本G0の対象外。完了報告が来たら別途監査する。

## 2. 付帯裁定①: ledger の #2/#4 採点済み履歴との重なり(§6指摘への回答)
**既存行は編集・削除しない**(append-only)。以下の是正行を追記して解決する:
- 旧 #2/#4 に対応する**訂正行**を追記: `{"serial":2,"status":"void_misfire",
  "note":"空投稿事故(2026-07-05〜06)に紐づく無効エントリ。fc5e427で巻き戻し",...}` の形式。
- 巻き戻し後の新 #2(分野B・本文入り)以降が正史。月次弱点マップの集計は
  status=void_misfire を除外する(集計コードに1行、テスト1本)。

## 3. 付帯裁定②: edinet系 400 の解釈と再プローブ
公式仕様により **edinet/major-shareholders・cross-shareholdings は API 経由のみ
(Bulk/CSV 非対応)**。/bulk/list 経由のプローブなら 400 は仕様どおりの失敗であり
プラン除外を意味しない。→ DT-1b の D0 のついでに **REST直**(x-api-key、
params: code=86970 等)で再プローブし、ステータスだけ報告(読み取り1リクエスト)。

## 4. GO(a): DT-1a の J-Quants 第2ソース化(小タスク・単独コミット)
- /equities/earnings-calendar を平日夕方に1回取得(T+1分のみという仕様を前提)。
- earnings_calendar_v1 に source=jquants_v2 で PIT追記(JPX行と並存)。
- daily-update に不一致検出1行: 「決算カレンダー照合: JPX vs J-Quants 不一致 N件」
  (0件なら出さない。不一致銘柄はログへ)。
- テスト: 同一銘柄で日付不一致→検出 / 片方欠損→不一致扱いにしない(UNKNOWN)。

## 5. GO(b): DT-1b 信用残の実装(Phase 1 本体)
- 手順: D0最小実測(margin-interest の horizon/公表ラグ/粒度を1〜2リクエストで)→
  実装 → 単独コミット → 確認依頼MD(ゲートG1)。
- 仕様は確定済みのとおり: dt1_spec.md §DT-1b + v2_full_strategy.md §3
  (margin-interest 主データ + margin-alert 規制フラグ / PIT追記型 / 週の欠測は正直に /
  観測とカバレッジ報告のみ・briefに出さない / シグナル化禁止)。
- **公表ラグの実測値を derived の as_of 設計に反映**し、確認依頼MDに明記すること。
- DT-1c は G1 通過まで着手禁止(従来どおり)。

## 6. ロードマップの現在地
- 現在地: **Phase 1(DT-1b)+ 小タスクGO(a)**。0-3(Codex)は完了報告待ち。
- ロードマップ/INDEXの改版は、第2監査役の初回所見と米国地合いM0(裁定済み・仕様未発行)を
  まとめて1回で行う(版ズレ防止のためのバッチ改版)。それまで本文書が現在地の正。

## 7. 次の確認依頼MD(G1)に含めるもの
- GO(a)のコミットhash+不一致検出の実例(あれば) / §2の訂正行の追記結果 /
  §3の再プローブ結果 / DT-1b: D0実測値(horizon・公表ラグ)→実装→テスト→
  実データ1回実行の目視 / git状態転記 / 逸脱申告。

規律不変: push無し / PIT / append-only / 売買推奨・ランキングなし / test期間封印 /
第2監査役の所見は直接実行しない。
