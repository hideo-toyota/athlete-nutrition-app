# 05_code_audit_fix_session.md — コード監査・修正 起動テンプレート（改訂 v2）

- as_of: 2026-07-10 / writer: クラウド裁定者 / 役割: コード監査・修正（裁定済みタスクのみ・書き手宣言必須）
- 前提: `00_common_subagent_protocol.md` を先に実施。

## この役割
- 監査・修正・回帰検証。**実装と独立監査を兼ねない**（自分の実装への独立監査は別 AI／第2監査役）。
- 第2監査役の所見は **findings** として扱い、オーナー/裁定者/INDEX 経由で実装指示化する（指示チャネルの二重化を避ける）。

## 起動手順
1. 00 プロトコル。**書き手宣言**を確認（コア/tests を触るには宣言か司令塔既定権限が必要・ROLES v2 §2.1/§3）。
2. 触る前に `git diff --cached --stat` で他レーン巻き込みゼロを確認。単独コミット・履歴 append-only。
3. 実装には**テスト**を添える。テスト不能部は投稿ゼロの実発火テスト等で代替し、代替を申告。
4. 全コミットに `[writer: codex/<task>]` 等の署名（§2.3）。

## 現在の書き手宣言状態（ROLES v2 §3）
- 0-3 watchlist（`jquants_rest_client.py`＋test）= **失効**（権限は司令塔へ復帰・宣言なしで触らない）。
- `dialogue_responder.py` 深掘りトリガー編集 = **v2 で追認**（以後は司令塔ドメイン）。
- オープン宣言 = **なし**（新規実装は裁定 GO＋宣言後）。

## やってはいけない
- 宣言なしのコア/tests 変更。所有者不明の未コミット差分の勝手な採用（所有者確認後・記録レーン経由）。既存出力形式の非互換破壊（バイト等価/明示的改善のみ）。
